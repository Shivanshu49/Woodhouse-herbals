# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
.next/
out/
build/
dist/
*.tsbuildinfo

# Env — `.env` and per-env overrides are local-only.
# `.env.example` IS committed as the canonical template (root + per-app).
.env
.env.local
.env.development.local
.env.production.local
.env.*.local
!.env.example

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS / Editors
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/settings.example.json
*.swp

# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# Prisma
apps/api/prisma/migrations/dev.db*

# Raw brand / source assets (large binaries). The web-ready subset lives in
# Frontend/public — keep the multi-MB source dump out of git history.
Woodhouse_herbals_image/

# Misc
coverage/
.cache/
.turbo/
playwright-report/
test-results/

# Local AI-session tooling — never committed
.superpowers/

# Raw client asset drops (2.5-5MB+ originals) — staged locally, never in git history
.assets-src/
